[user]
	name = Sagi Schwarz
	email = mail@sagischwarz.xyz
	signingkey = 1EE83B72F09B272B
[credential]
	helper = store
[core]
	fileMode = false
	autocrlf = input
	pager = delta
[interactive]
	diffFilter = delta --color-only
[delta]
	navigate = true
	syntax-theme = Nord
	line-numbers = true
	side-by-side = true
[color]
	ui = auto
[alias]
	dt = difftool
	lgng = log --branches --tags --color --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an [%G?]>%Creset' --abbrev-commit
	lg = lgng --graph
	lga = lg --all
	fm = lgng --all -i -F --grep
	fc = lgng --all -i -S
[diff]
	tool = meld
	colorMoved = default
	algorithm = histogram
[merge]
	tool = meld
	conflictStyle = zdiff3
[difftool]
	prompt = false
[pull]
	rebase = true
[pager]
	branch = false
[commit]
	gpgsign = true
[init]
	defaultBranch = main
